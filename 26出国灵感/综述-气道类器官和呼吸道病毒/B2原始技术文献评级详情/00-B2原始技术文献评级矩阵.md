# 评价维度说明 (C1–C9)

| 代码     | 维度名称      | 评价重点                                               |
| ------ | --------- | -------------------------------------------------- |
| **C1** | 细胞组成完整性   | 目标气道区域所需的主要谱系(基底/纤毛/杯状/Club/AT2/AT1等)是否齐全、比例是否接近体内 |
| **C2** | 空间结构与极性   | 是否形成管腔/分支结构、顶底极性是否正确(纤毛朝腔/基底贴基底膜)、紧密连接是否连续         |
| **C3** | 生理功能表现    | 黏液纤毛清除、屏障功能(TEER)、病毒/药物感染应答、表面活性物质分泌等直接功能测定        |
| **C4** | 来源与发育身份   | 细胞来源、物种、气道区域、发育阶段、供体状态、采样方式六轴分类标签(不评分)             |
| **C5** | 成熟度与体内一致性 | 转录组/蛋白组/空间组学与同源体内组织的多模态相似度、成熟标志物动态                 |
| **C6** | 重复性与标准化   | 跨供体/批次/实验室/细胞系的量化稳定性、QC指标完整性、SOP标准化程度              |
| **C7** | 样本代表性     | 供体队列对年龄/性别/种族/疾病状态/气道区域的覆盖度、外部队列验证                 |
| **C8** | 微环境适配性    | 基质/生长因子/机械/免疫/血管等微环境成分与研究问题的匹配度、功能验证               |
| **C9** | 扩增稳定性与规模化 | 长期扩增能力、冻存复苏、身份功能保持、生物反应器/GMP级规模化潜力                 |
> **评分规则**：1–10分为当前技术标准绝对评分，六锚点达标式；仅 `#已评分` 对应数值。`#无法判定_未涉及`、`#无法判定_补充材料缺失`、`#不适用_研究目标外` 记为 `null` 不计入总分。C4 为分类维度不评分。**≥9分以红色高亮显示**。
---
## 技术表现矩阵 (C1–C3, C5–C9 + 总分)
```dataviewjs
const pages = dv.pages('"26出国灵感/综述-气道类器官和呼吸道病毒/B2原始技术文献评级详情"')
  .where(p => p.file.name !== dv.current().file.name)
  .sort(p => p.file.name);

const dims = ["C1","C2","C3","C5","C6","C7","C8","C9"];
const dimLabels = {
  C1: "细胞组成", C2: "空间结构", C3: "生理功能",
  C5: "成熟一致", C6: "重复标准", C7: "样本代表", C8: "微环境适配", C9: "扩增规模"
};

function fmtScore(page, dim) {
  const score = page[dim + "_评分"];
  const status = page[dim + "_状态"];
  if (score != null && typeof score === "number") {
    const cls = score >= 9 ? 'style="color:#c00;font-weight:bold;"' : "";
    return `<span ${cls}>${score}</span>`;
  }
  return `<span style="color:#666;font-size:0.9em;">${status || "—"}</span>`;
}

function totalScore(page) {
  let sum = 0, cnt = 0;
  for (const dim of dims) {
    const s = page[dim + "_评分"];
    if (s != null && typeof s === "number") { sum += s; cnt++; }
  }
  return cnt ? sum : "—";
}

const headers = ["文献", ...dims.map(d => dimLabels[d]), "总分"];
const rows = pages.map(p => [
  p.file.link,
  ...dims.map(d => fmtScore(p, d)),
  totalScore(p)
]);

dv.table(headers, rows);
// TOKENS: C1_评分 C9_状态 C4_类型标签
```

---

## C4 来源与发育身份矩阵

```dataview
TABLE WITHOUT ID
file.link AS 文献,
评估对象 AS 主要技术配置,
join(C4_类型标签, " ") AS C4来源与发育身份
FROM "26出国灵感/综述-气道类器官和呼吸道病毒/B2原始技术文献评级详情"
WHERE file.name != this.file.name
SORT file.name ASC
```
