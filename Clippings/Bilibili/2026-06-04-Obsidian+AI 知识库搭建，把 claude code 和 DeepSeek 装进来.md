---
title: "Obsidian+AI 知识库搭建，把 claude code 和 DeepSeek 装进来"
url: "https://www.bilibili.com/video/BV1bj5v6JEFA?spm_id_from=333.788.player.player_end_recommend&trackid=web_related_0.router-related-2479604-6dnm7.1780521022062.1011&vd_source=e3b325d73246032e5c1b1a4886f3e3f3"
bvid: "BV1bj5v6JEFA"
cid: "38352259553"
author: "小陈同学c_z"
upload_date: "2026-05-15"
subtitle_lang: "中文"
created: "2026-06-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116577928288496&bvid=BV1bj5v6JEFA&cid=38352259553&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

这期视频，我把 Claude Code + DeepSeek V4 + Obsidian 串成了一套完整工作流：从本地 AI 配置、Obsidian 插件接入，到网页内容保存、字幕整理、白板生成，全部放进一个知识库里完成。 #obsidian #ai教程 #claude 
视频内容主要分为 4 个部分：
第一，安装claude code + 购买 DeepSeek api，并完成配置；
第二，介绍 2 款 Obsidian 插件实现 AI 接入（terminal 和claudian）；
第三，演示 Ai + Obsidian web clipper 获取网页内容并总结
第四，安装 excalidraw 和 skill，一键完成白板画布绘制

## 章节

- `00:00` 开头介绍
- `00:41` 环境配置
- `01:53` 安装 Obsidian 插件
- `04:26` 知识采集
- `06:24` 画布绘制
- `07:36` 片尾福利

## 字幕

### 开头介绍 `00:00`

`00:00` 本期视频分享
`00:01` Obsidian+claude code+deepseek v4
`00:03` 搭建 Obsidian+AI 的本地知识库
`00:06` 我在上期视频介绍了
`00:08` 将 deepseek v4 接入 claude code 的详细步骤
`00:11` 这期视频把这套组合打包放进 Obsidian 中
`00:14` 让AI 完成内容总结
`00:16` 文案撰写等工作
`00:17` 并且还可以 让他画出
`00:19` 我屏幕上展示的 这些  手绘风格的画板
`00:22` 视频内容主要分为 4 个部分：
`00:24` 第一
`00:24` 安装claude code + 购买 DeepSeek api
`00:27` 并完成配置
`00:28` 介绍 2 款 Obsidian 插件实现 AI 接入
`00:32` 第三
`00:32` 演示 Ai + Obsidian web clipper 获取网页内容并总结
`00:37` 第四，安装 excalidraw 和 skill
`00:39` 一键完成白板画布绘制

### 环境配置 `00:41`

`00:42` 第一步
`00:43` 安装claude code + 购买 DeepSeek api
`00:46` 在我的上期视频有非常完整的介绍
`00:48` 你可以直接去看上期视频
`00:50` 不过我在这里可以快速过下
`00:52` 首先到 claude code 项目官网
`00:54` 复制你电脑类型的的一键安装命令；
`00:57` 支持 macos，linux 和 Windows
`00:59` Windows 用户需要安装 git
`01:01` 直接到 git 官网下载安装包安装即可
`01:04` 第二步 打开电脑终端应用
`01:06` 粘贴 执行
`01:08` 执行完毕之后你就可以使用 claude code
`01:10` 可以通过 claude 命令启动他
`01:12` 但是他还需要接入第三方 api 模型
`01:15` 第三步
`01:15` 我们来安装一个 ccswitch 的开源工具
`01:18` 他可以帮你把第三方大模型接入 claude code
`01:21` 在项目的首页有下载安装包的地址
`01:23` 找到对应的安装包，下载安装
`01:26` 第四步
`01:26` 到 DeepSeek api 官网购买 10 块钱额度的 api
`01:29` 然后创建一个 api key；
`01:32` 最后打开 ccswitch这个软件
`01:34` 点击右上角+号
`01:36` 找到 deepseek 供应商
`01:37` 下滑粘贴api key
`01:39` 模型这里复制我填写的模型名称
`01:41` 点击保存
`01:42` 然后点击测试模型按钮
`01:44` 显示运行正常，之后点击启用就行了
`01:48` 然后在终端应用，输入 claude 指令
`01:50` 那你的第三方模型就可以正常使用 claude code 了

### 安装 Obsidian 插件 `01:53`

`01:54` 视频第二部分
`01:55` 介绍 2 款 Obsidian 插件都能实现 调用 AI 功能
`02:01` 根据个人的需求选择
`02:03` 先来介绍 terminal
`02:04` 他可以在 Obsidian 内部
`02:06` 直接打开一个终端窗口 来执行命令行操作
`02:09` 比如调用 claude code 而不离开Obsidian
`02:12` 直接在第三方插件市场搜索这个名称安装即可
`02:16` 安装好之后
`02:17` 左侧会出现 open terminal 这个 logo
`02:19` 点击一下，会有三个使用方式可选
`02:23` 这三种模式对应三种不用的场景
`02:26` 外部模式：
`02:26` 他是在 Obsidian 之外启用一个 终端应用
`02:29` 所以会脱离 Obsidian 的环境
`02:31` 所以不太需要
`02:32` 整合式：
`02:33` 他是在 Obsidian 内部启动一个终端应用
`02:36` 所以最推荐
`02:38` 第三种开发者控制台
`02:39` 主要是给开发者调试插件使用
`02:41` 所以也不推荐
`02:43` 我们在这里点击 整合式
`02:45` 右侧侧边栏会出现一个 终端程序
`02:48` 这个终端所在文件夹就是当前知识库的位置
`02:52` 输入 claude 指令
`02:53` 就能直接调用 claude code
`02:55` 比如我提问：
`02:56` 统计下当前知识库一共有多少文章
`02:59` 多少个字
`03:00` 他在侧边框这里执行任务指令
`03:02` 如果需要权限许可
`03:04` 也会提醒
`03:05` 他可以阅读文章内容，写文案
`03:07` 安装 skill 等等操作
`03:09` 和终端的使用体验是一样的
`03:11` 区别是 不需要额外开启一个单独的终端窗口
`03:15` 另一个插件是是 claudian
`03:17` 在我准备视频的时候他还没上线第三方插件市场
`03:20` 但是现在好像已经上架了
`03:22` 可以直接在这里安装
`03:24` 如果你搜索不到
`03:25` 这里介绍另一种方式安装
`03:27` 直接打开claudian的 github 仓库
`03:30` 在Release发布页面
`03:32` 需要下载这三个文件：
`03:34` js
`03:35` json，和 css
`03:37` 然后回到 Obsidian，第三方插件这里
`03:39` 点击打开 插件 文件夹
`03:41` 这里都是 你在 Obsidian 中安装的插件
`03:44` 在这里新建一个文件夹
`03:46` 名称就叫做claudian，比如我这个
`03:48` 直接把刚刚三个 文件移动过来
`03:51` 然后重启一下 Obsidian
`03:53` 在第三方插件里 启用Claudian
`03:56` 左侧应该会有一个 open claudian 的图标
`03:59` 点击下
`04:00` 右侧就会出现 claudian 的对话框
`04:02` 不需要进行任何的设置
`04:04` 只要本地安装了 claude code 就可以使用
`04:06` 我这里已经有了一些历史对话
`04:09` 他的界面比 terminal 是更美观的
`04:12` 对话时会默认选中当前正在看的Markdown 文件
`04:15` 右下角有三个按钮
`04:16` 分别是开启新的 tab 页面、开启新对话以及查看历史的对话
`04:21` 我推荐使用 claudian
`04:23` 比terminal的适配度更高
`04:25` 更方便使用
`04:25` 第三

### 知识采集 `04:26`

`04:27` claude code 在每个工作的项目文件夹都会有一个claude
`04:31` md 文件
`04:33` 用来指导 AI 如何在这个项目里工作
`04:35` 一个完整的claude
`04:37` md 文件可能要包含这些内容
`04:39` 但这个是给开发用的
`04:41` 个人简单使用，只需要制定规范
`04:43` 明确输出要求，和输出格式就行了
`04:46` 不需要写得很复杂
`04:48` 我的 CLAUDE
`04:49` md  里面包括简单的仓库定位
`04:52` 目录结构
`04:52` 输出规范等等
`04:54` 因为我可能会获取一些英文的内容
`04:56` 所以会有翻译的规范等等
`04:57` 每个人的claude
`04:59` md 根据个人的需求来不断修改
`05:01` 在看一些博客网站的时候
`05:03` 可以通过 官方的 Obsidian web clipper
`05:06` 拓展插件来将网页内容保存成 md 格式
`05:10` 并存在 Obsidian 中
`05:11` 并且是保留了 章节信息
`05:13` 配图等主要内容
`05:15` 其实视频的内容也可通过这个插件获取到字幕文件
`05:19` 获取到之后
`05:19` 因为是英文字幕 并且 是口语化的表达
`05:22` 后期翻阅比较麻烦
`05:24` 所以需要 AI 帮我整理
`05:26` 只需要在 claudian对话框输入
`05:29` “请帮我翻译并格式化这篇文档”
`05:31` 他会默认选中这篇文档
`05:34` 然后按照我给他制定的规范完成翻译
`05:36` 整理
`05:37` 文档命名等操作
`05:38` 直接来看他最终完成的情况：
`05:40` 一个是四个任务
`05:42` 首先翻译成中文，格式化内容展示
`05:45` 把新文档移动到指定目录
`05:47` 把原文档归档
`05:48` ok，这个就是 翻译整理后的内容
`05:51` 告别了视频字幕口语化的表达
`05:53` 并且完整的信息也保留了下来
`05:55` 我后面想要查看这个视频内容就变得简单了许多
`05:58` 尤其是当我想要写一个关于 xx 主题的教程的时候
`06:02` 可以直接让AI帮我把 知识库里面
`06:05` 所有关于 xx 内容 筛选出来整理成一篇完整的文档
`06:09` 但是这个拓展插件不支持某站的字幕下载
`06:13` 所以我有专门开发一个专门的 插件
`06:15` 他可以获取视频的字幕文件
`06:17` 然后一键保存到 Obsidian 中
`06:19` 并且我把他开源到了 github 上
`06:21` 感兴趣的话可以去使用

### 画布绘制 `06:24`

`06:25` 我之前的视频里 会用到 一个白板工具
`06:27` 用来展示 整个视频的章节信息  或者 某个功能对比
`06:31` 这个白板叫做 excalidraw
`06:33` 是一个主打「手绘风格」的在线白板 / 绘图工具
`06:36` 他有单独的网页版
`06:38` 在 Obsidian 中也可以使用
`06:40` 并且我觉得在 Obsidian 中可能更方便使用
`06:43` 因为可以使用 ai 来生成内容
`06:45` 我们首先需要安装一个 Obsidian插件
`06:48` 名称就叫做 excalidraw
`06:50` 然后我们就可以在 文件 里面 新建一个白板
`06:53` 在这里可以进行内容的编排
`06:55` 可以插入图片 或者 输出这种手绘风格的文字
`07:00` 但是我们想让 ai 帮我们直接生成的话
`07:02` 这里就需要安装一个 skill
`07:04` 我安装的这个 skill 叫做 excalidraw-diagram
`07:08` 安装方式也很简单
`07:08` 直接复制 这个 skill 的仓库地址
`07:10` 给到 claude code，然后说
`07:12` 请帮我安装这个 skill 就可以了；
`07:15` 安装好之后
`07:15` 可以通过/命令 显性的调用这个 skill；
`07:19` 比如让他把以下内容 生成一个画布
`07:22` 内容是我刚刚从网页上获取到的内容
`07:24` 关于 deepseek v4 的任务表现情况
`07:27` 等待一会，他就会生成一个内容
`07:29` 一个简单的 deepseek v4适用场景分析就做好了
`07:33` 如果需要修改直接在这里编辑就可以了

### 片尾福利 `07:36`

`07:37` 在之前的一期分享 Obsidian 获取视频内容 的教程里
`07:40` 有人让人分享我的 claude
`07:43` md文档
`07:44` 我会把我的配置保存成一个 在线文档
`07:46` 包含这期视频提及到的所有产品
`07:49` 一键三连私信我 Obsidian 获取
`07:52` ok，以上就是本期视频的全部内容
`07:55` 我们下期再见
`07:56` 拜拜