```dataview
TABLE infoValue AS "价值", dateformat(file.ctime, "yyyy-MM-dd") AS "创建时间"
FROM "02 知识管理"
WHERE (status = "未开始" OR status = "进行中")
AND contains(tags, "视频选题")
// 前面的条件优先排序
SORT infoValue DESC, file.ctime DESC, file.name ASC
```
