```dataview
TABLE noteType AS "类型", status AS "状态"
FROM "02 知识管理/00 灵感库"
WHERE (status = "未开始" OR status = "进行中")
AND !regextest("(图文|视频)选题", tags)
// 前面的条件优先排序
SORT infoValue DESC, file.ctime DESC, noteType DESC, file.name ASC
```
