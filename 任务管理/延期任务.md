```tasks
# 【数据查找逻辑】
## 仅查找未完成的任务
not done

## 仅查找本周及之前计划完成、截止的任务；但允许展示未完成的创作及家务
## ((scheduled before this week) OR (due before this week)) AND NOT ((tags include #创作) OR (tags include #家务))
(scheduled before this week) OR (due before this week)

## 只查找计划状态为“进行中”的任务
filter by function ["进行中"].includes(task.file.property("status"))


# 【数据展示逻辑】
## 按文件名分组
group by filename reverse

## 按任务紧急程度降序
sort by urgency

## 显示/隐藏紧急程度数值
show urgency

## 显示/隐藏任务来源
show backlink

## 显示/隐藏循环生成规则
hide recurrence rule
```
